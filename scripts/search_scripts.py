"""Legacy ``scripts`` module placeholder.

The high-level automation APIs now live under ``weBot``. Import directly from
``weBot`` modules (for example ``weBot.workflows``) rather than the deprecated
``scripts`` package.
"""

from __future__ import annotations


raise ImportError(
    "The 'scripts.search_scripts' module has been removed. Import from the"
    " supported 'weBot' packages instead."
)
